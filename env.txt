# JWT Secret Key (generate a secure key)
JWT_SECRET_KEY=your-secret-key

# Azure AD Configuration
AZURE_AD_TENANT_ID=your-tenant-id
AZURE_AD_CLIENT_ID=your-client-id

# Azure Key Vault
KEY_VAULT_URL=https://your-vault.vault.azure.net/

# Azure Sentinel (mock for development)
SENTINEL_ENDPOINT=https://mock-sentinel-api.azure.com/log
SENTINEL_TOKEN=mock-token

# Test Token for Postman and Locust
CUSTOMER_TOKEN=your-customer-token